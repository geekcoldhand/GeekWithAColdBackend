{
	"version": 2,
	"builds": [
		{
			"src": "package.json",
			"use": "@vercel/next",
			"config": {
				"distDir": "src/.next"
			}
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "src/$1"
		}
	]
}
